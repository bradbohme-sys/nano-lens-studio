import { DSLRInterface } from "@/components/DSLRInterface";

const Index = () => {
  return (
    <main className="h-screen w-full overflow-hidden bg-background">
      <DSLRInterface />
    </main>
  );
};

export default Index;